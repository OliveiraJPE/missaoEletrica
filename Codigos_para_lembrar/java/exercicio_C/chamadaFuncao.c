#include <stdio.h>
#include <stdlib.h>


float t(float);

void main(){
float calc = t(float);//(((int)(tempo()/60))*2) + ((tempo()/60)- ((int)(tempo()/60))*0.50);
printf("Retorno: %f", calc);
return 0;
}

float t(float)){
    float time;
    time=166.00
 return time;
}
