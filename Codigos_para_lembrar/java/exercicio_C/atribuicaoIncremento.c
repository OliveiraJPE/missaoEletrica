#include <stdio.h>
#include <stdlib.h>

int main()
{
  //Operadores de atribuição
  /*
  x=x+1;  => x++; => x+=1;

  a) +=
  b) -=
  c)*=
  d) /+
  e)%= resto da divisão
  */
  int i=50;

    printf(" soma: %i\n", i+=100);
    return 0;
}
