#Truncate table apaga todas as linhas da tabela.
#DDL - Data Definition Language - create database, create table, alter table, drop table.
#DMl - Data modify language - insert into, update, delete, truncate.
#drop table apaga a tabela, trncate apaga os dados(tuplas), mas mantem a tabela.


truncate table cursos;  # ou truncate cursos. apaga todas as linhas. NÃo executei....